# Absolute site URL used for canonical links, sitemap, and RSS generation.
SITE_URL=https://conflictedape.dev

# Base path the site is served under (e.g. "/" for a root deploy, or
# "/my-repo" for a GitHub Pages project site).
BASE_PATH=/
